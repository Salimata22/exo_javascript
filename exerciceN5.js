function BaliseNom(obj) {
    var nom = Object.keys(obj)
    var pays = Object.values(obj)
    // var tab = Object.entries(obj)

    
    for (var i = 0; i < nom.lengt; i++) {
        for (var j = 0; j < pays.lengt; j++) {
            var a = pays[i]
            var b = pays[j]
          var resultat =  ("Hi! I'm "+{a}+ ", and I'm from "+{b})     
        }
    }
    return resultat
}

 

  console.log(BaliseNom(GUEST_LIST = {
    Randy: "Germany",
    Karla: "France",
    Wendy: "Japan",
    Norman: "England",
    Sam: "Argentina"
  }

  ));
                       

  